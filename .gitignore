node_modules/
.DS_Store
.next/
*.tsbuildinfo

# Local environment files — real values must not be committed.
# .env.example is the tracked template.
.env
.env.local
.env*.local
!.env.example

# Build output: WebP variants generated by scripts/optimize-images.mjs.
# Regenerated by `npm run build` in ~22s, so not worth 30MB in the repo.
public/optimized/

# Deployment provider caches
.vercel
.netlify
